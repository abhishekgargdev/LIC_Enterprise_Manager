import mongoose from "mongoose"
const claimSchema = new mongoose.Schema({ claimNumber: { type: String, unique: true, required: true }, policy: { type: mongoose.Schema.Types.ObjectId, ref: "Policy", required: true }, customer: { type: mongoose.Schema.Types.ObjectId, ref: "Customer", required: true }, claimType: { type: String, enum: ["MATURITY", "DEATH", "SURRENDER", "RIDER"], required: true }, claimAmount: { type: Number, required: true }, description: String, filedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true }, filedDate: { type: Date, default: Date.now }, status: { type: String, enum: ["PENDING", "UNDER_REVIEW", "APPROVED", "REJECTED", "SETTLED"], default: "PENDING" }, approvedAmount: Number, settledDate: Date, rejectionReason: String, reviewedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User" } }, { timestamps: true })
export const Claim = mongoose.models.Claim || mongoose.model("Claim", claimSchema)
