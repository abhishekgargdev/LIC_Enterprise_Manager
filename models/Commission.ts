import mongoose from "mongoose"
const schema = new mongoose.Schema({ premium: { type: mongoose.Schema.Types.ObjectId, ref: "Premium", required: true, unique: true }, policy: { type: mongoose.Schema.Types.ObjectId, ref: "Policy", required: true }, agent: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true }, manager: { type: mongoose.Schema.Types.ObjectId, ref: "User" }, branch: { type: mongoose.Schema.Types.ObjectId, ref: "Branch", required: true }, agentAmount: Number, managerAmount: Number, branchAmount: Number, premiumAmount: Number, calculatedAt: { type: Date, default: Date.now }, status: { type: String, enum: ["PENDING", "CREDITED"], default: "PENDING" } }, { timestamps: true })
export const Commission = mongoose.models.Commission || mongoose.model("Commission", schema)
