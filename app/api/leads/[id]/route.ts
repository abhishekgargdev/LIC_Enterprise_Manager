import { NextResponse } from "next/server"
import { connectDB } from "@/lib/db"
import { getSession } from "@/lib/auth"
import { Lead } from "@/models/Lead"
export async function GET(_: Request, { params }: { params: Promise<{ id: string }> }) { await connectDB(); const session = await getSession(); const { id } = await params; if (!session) return NextResponse.json({ success: false, error: "Unauthorized" }, { status: 401 }); const lead = await Lead.findById(id).populate("notes.createdBy", "name"); if (!lead) return NextResponse.json({ success: false, error: "Lead not found" }, { status: 404 }); return NextResponse.json({ success: true, data: lead }) }
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { await connectDB(); const session = await getSession(); const { id } = await params; if (!session) return NextResponse.json({ success: false, error: "Unauthorized" }, { status: 401 }); const body = await request.json(); if (body.stage === "LOST" && !body.lostReason) return NextResponse.json({ success: false, error: "A lost reason is required." }, { status: 400 }); const update: Record<string, unknown> = { ...body }; if (body.note) { update.$push = { notes: { text: body.note, createdBy: session.userId, createdAt: new Date() } }; delete update.note } const lead = await Lead.findByIdAndUpdate(id, update, { new: true }); return NextResponse.json({ success: true, data: lead }) }
